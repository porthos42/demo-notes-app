{
  "name": "notes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.60.6",
    "@serverless-stack/resources": "0.60.6",
    "aws-cdk-lib": "2.7.0",
    "aws-sdk": "^2.971.0",
    "dotenv": "^15.0.0",
    "stripe": "^8.201.0",
    "uuid": "^7.0.3"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^3.12.2",
    "typescript": "4.4.4"
  }
}
